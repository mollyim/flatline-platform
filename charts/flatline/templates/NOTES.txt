Thank you for installing {{ title .Chart.Name }}. Your Helm release is named "{{ .Release.Name }}".

Kubernetes resources part of this release will be prefixed with "{{ include "common.fullname" .}}".

---

🚧 WARNING 🚧

The Flatline project is a work in progress.

This chart and its resources are unstable in order to facilitate development.

During development, several features will be disabled or insecurely implemented.

Do not run this in production environments.

---

Please, wait for the Whisper service to become ready. You can monitor this with the following command:

$ kubectl wait --timeout=5m -n {{ .Release.Namespace }} deployment/{{ include "common.fullname" .}}-whisper-service --for=condition=Available

{{ if .Values.traefikResources.enabled -}}
Since you have chosen to use the development Traefik resources, you can find the relevant Flatline endpoints here:

{{ if .Values.whisperService.enabled -}}
- Whisper Service: https://whisper.{{ .Values.global.hostname }}
{{ if .Values.global.exposeAdmin -}}
- Whisper Service Admin: https://whisper-admin.{{ .Values.global.hostname }}
{{ end -}}
{{ end -}}
{{ if .Values.storageService.enabled -}}
- Storage Service: https://storage.{{ .Values.global.hostname }}
{{ if .Values.global.exposeAdmin -}}
- Storage Service Admin: https://storage-admin.{{ .Values.global.hostname }}
{{ end -}}
{{ end -}}
{{ if .Values.contactDiscoveryService.enabled -}}
- Contact Discovery Service: https://cds.{{ .Values.global.hostname }}
{{ end -}}
{{ if .Values.callingServiceFrontend.enabled -}}
- SFU: https://sfu.{{ .Values.global.hostname }}
{{ end -}}
{{ if .Values.localstack.enabled -}}
- CDN0: https://cdn0.{{ .Values.global.hostname }}
{{ end -}}
{{ if .Values.tus.enabled -}}
- CDN3: https://cdn3.{{ .Values.global.hostname }}
{{ end -}}
{{ else -}}
Since you have chosen to use your own ingress gateway, ensure that the relevant Flatline endpoints are exposed:

{{ if .Values.whisperService.enabled -}}
- Whisper Service on port {{ .Values.whisperService.service.ports.default.port }}.
{{ end -}}
{{ if .Values.storageService.enabled -}}
- Storage Service on port {{ .Values.storageService.service.ports.default.port }}.
{{ end -}}
{{ if .Values.contactDiscoveryService.enabled -}}
- Contact Discovery Service on port {{ .Values.contactDiscoveryService.service.ports.default.port }}.
{{ end -}}
{{ if .Values.callingServiceFrontend.enabled -}}
- Calling Service Frontend (SFU) on port {{ .Values.callingServiceFrontend.service.ports.default.port }}.
{{ end -}}
{{ if .Values.localstack.enabled -}}
- Localstack (CDN0) on port {{ .Values.localstack.service.ports.default.port }}.
{{ end -}}
{{ if .Values.tus.enabled -}}
- Tus (CDN3) on port {{ .Values.tus.service.ports.default.port }}.
{{ end -}}

These endpoints need to be reachable by clients through TLS.
{{ end -}}
